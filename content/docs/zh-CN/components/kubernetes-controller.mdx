---
title: Kubernetes Controller
description: 打通 Kubernetes Service 同步与 Pole Sidecar 自动注入。
---

Kubernetes Controller 用于把 Kubernetes 生态接入 Lattice Hub。它提供两个核心方向：K8s Service 同步到服务治理控制面，以及 Pole Sidecar 自动注入。

## Service 同步模式

- `all`：全量同步，将 K8s Service 全部同步到服务治理控制面。
- `demand`：按需同步，默认不同步，需要在 Namespace 或 Service 上添加 annotation。

生产共享集群优先 `demand`，避免系统噪声服务进入治理目录。

## 常用 annotation

| 注解 | 说明 |
| --- | --- |
| `polarismesh.cn/sync` | 是否同步服务，`true` 同步，`false` 不同步 |
| `polarismesh.cn/aliasService` | 同步时创建的服务别名 |
| `polarismesh.cn/aliasNamespace` | 服务别名所在命名空间 |

## 部署入口

```bash
make build

# 静态清单
cd deploy/kubernetes_v1.22/kubernetes && bash install.sh

# Helm
cd deploy && sh init_helm.sh
cd kubernetes_v1.22/helm && helm upgrade -i pole-controller .
```

部署前配置控制面地址与 token（见 `deploy/variables.txt`）。验证：

```bash
kubectl get pods -n polaris-system
```

## Pole Sidecar 注入

Controller 支持给应用 Pod 自动注入 sidecar：

- `dns`：通过拦截 DNS 请求实现服务发现和治理。
- `mesh`：注入 sidecar 和 Envoy，通过劫持流量实现低侵入服务治理。

注入前确保控制面已有对应服务与所需 active 规则。

## 推荐阅读

- [K8s 相关实践](/docs/practices/kubernetes-sync)
- [Kubernetes Service 到 Lattice Hub 的同步模型](/docs/blog/kubernetes-service-sync)
