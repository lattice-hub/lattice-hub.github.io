---
title: Control Plane
description: Lattice Hub 的核心控制面，内嵌 Console，承载服务发现、配置中心、治理规则和 AI Native Registry。
---

Control Plane 是 Lattice Hub 的核心组件。它组织 API、业务逻辑、缓存和存储，向外提供 HTTP、gRPC、xDS、Nacos、Apollo、Eureka 等多协议入口；Console 与 Limiter 已合并在同一仓库与制品中，不再作为独立生态组件列出。

![deployment architecture](/diagrams/deployment-architecture.svg)

## 关键职责

- 服务发现：管理命名空间、服务、实例和健康状态。
- 配置中心：支持配置发布、灰度、回滚、SSE 或长轮询监听。
- 治理规则：承载路由、泳道、限流、熔断、故障探测、无损上下线、调用鉴权、流量镜像和流量 Mock。
- AI Native：MCP Registry 与 A2A Agent Registry 已具备存储、缓存、HTTP API 和 Console 管理入口。
- Pole Agent：通过真实 LLM/MCP 最小闭环读取资源，并将配置修改限制在预览、确认和草稿边界内。
- 系统配置与观测：提供 63 项类型化配置目录、OTel 指标出口和共享 GreptimeDB 查询。
- 权限控制：资源支持细粒度鉴权，对标云厂商 CAM/RAM 类型能力。
- 内嵌 Console：Fluent UI 管理界面，覆盖资源、治理、权限、Registry、系统配置与监控指标。

## 运行模式

Control Plane、Console 与 Limiter 同仓交付，通过 `--mode` 组合部署。统一制品不等于强制同进程：生产环境仍可把 `control-plane`、`console`、`limiter-server` 分成独立 workload。

| 模式 | 作用 |
| --- | --- |
| `all` | Control Plane API + 内嵌 Console（本地默认产品入口） |
| `full` | Control Plane + Limiter + Console 同进程启动 |
| `control-plane` | 仅 Control Plane API / 协议入口 |
| `limiter-server` | 仅 Limiter 运行时（默认内部 gRPC `8101`）；生产推荐与控制面分进程 |
| `console` | 仅 Console 网关，通过网络访问 `poleServer.address` |
| `server` | `control-plane` 的弃用兼容别名 |

CLI 的 `--mode` 优先于 YAML 中的 `bootstrap.mode`。逐步操作见 [控制台使用](/docs/guides/console)。界面示例：

![配置分组](/images/console/config-groups.png)

![治理工作台](/images/console/governance.png)

![平台监控](/images/console/platform-metrics.png)

## 默认端口

接口与端口的权威说明见 [API 与协议](/docs/api)。常用默认值：

| 入口 | 端口 |
| --- | ---: |
| Console HTTP | 8080 |
| HTTP API | 8090 |
| Discovery gRPC | 8091 |
| Config gRPC | 8093 |
| xDS v3 | 15010 |
| Eureka / Nacos / Apollo | 8761 / 8848 / 8890 |

## 已确认工程事实

- 技术栈包括 Go、`go-restful/v3`、gRPC、MySQL、`envoyproxy/go-control-plane`、`mark3labs/mcp-go`、OpenTelemetry 与 Pebble；Console 前端为 React 18 + Fluent UI v9。
- 控制面按协议适配层、领域 Server、interceptor、缓存层、存储层装配，而不是单一 HTTP CRUD。
- 缓存层覆盖服务、配置、治理规则和 AI Registry 等资源，后台默认每 1 秒增量刷新。
- 增量查询窗口使用 `lastMtime - 5s` 规避时钟偏差。

## 适合继续深入的原理

- [控制面装配架构](/docs/principles/architecture)
- [增量缓存与事件流](/docs/principles/cache-eventhub)
- [治理规则与灰度发布](/docs/principles/governance-release)
- [鉴权链与资源映射](/docs/principles/auth-protocols)
- [AI Registry 与 Pole Agent](/docs/principles/ai-registry)
- [服务端安装](/docs/guides/server-install)
- [控制台使用](/docs/guides/console)
