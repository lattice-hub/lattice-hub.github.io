---
title: 命名空间
description: 环境维度的 Namespace CRUD。
full: true
---

环境维度的 Namespace CRUD。

默认 Base URL：`http://127.0.0.1:8090`。管理面接口需 `Authorization`（登录返回的 token）。

<HttpOpenApiReference section="namespaces" />
